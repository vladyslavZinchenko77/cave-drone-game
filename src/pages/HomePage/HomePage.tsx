import { FC } from 'react';
import GameSetup from '../../components/GameSetup/GameSetup';

const HomePage: FC = () => {
  return (
    <div>
      <GameSetup />
    </div>
  );
};

export default HomePage;
